import { useEffect } from 'react';


const Navigation = () => {
    // Initialize AOS
    useEffect(() => {
        AOS.init();
    }, []);

    return (
       
    );
};

export default Navigation;
